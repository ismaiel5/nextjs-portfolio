import Styles from '../../styles/Home.module.scss';
const Home = (props: any) => {
  return (
    <main className={Styles.container}>
      <h1>Home page</h1>
    </main>
  );
};

export default Home;
